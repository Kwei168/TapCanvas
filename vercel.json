{
  "version": 2,
  "installCommand": "pnpm install",
  "buildCommand": "pnpm --filter @tapcanvas/api prisma:generate",
  "builds": [
    { "src": "api/[...slug].ts", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/api/$1" }
  ]
}
