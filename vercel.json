{
  "version": 2,
  "installCommand": "pnpm install",
  "buildCommand": "pnpm --filter @tapcanvas/api prisma:generate",
  "builds": [
    { "src": "api/index.ts", "use": "@vercel/node" }
  ],
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/$1" }
  ]
}
